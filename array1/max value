//find max value in an array

#include<iostream>
using namespace std;

int main() {
	int n;
	cin>>n;
	long long arr[n];
	long long max;
	for (int i=0; i<n; i++){
		cin>>arr[i];
		if (i==0){
			max=arr[i];
		}else if (max<arr[i]){
			max=arr[i];
		}
	}
	cout<<max;
	return 0;
}
